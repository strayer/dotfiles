{
  "$schema": "./nono-profile.schema.json",
  "extends": "always-further/codex",
  "meta": {
    "name": "codex-cli"
  },
  "groups": {
    "include": []
  },
  "workdir": {
    "access": "readwrite"
  },
  "filesystem": {
    "allow": [
      "$HOME/.cache/uv"
    ],
    "read": [
      "$HOME/.global_gitignore",
      "$HOME/.bin/secretive-ssh-keygen",
      "$HOME/.config/gh",
      "$HOME/.config/mise",
      "$HOME/.config/uv/uv.toml",
      "$HOME/.local/share/mise",
      "$HOME/.local/state/mise",
      "$HOME/.ssh/known_hosts"
    ],
    "bypass_protection": [
      "$HOME/.ssh/known_hosts"
    ]
  }
}
