{
  "$schema": "./nono-profile.schema.json",
  "extends": "codex-cli-base",
  "meta": {
    "name": "codex-cli"
  },
  "filesystem": {
    "deny": [
      "/var/run/docker.sock",
      "$HOME/.orbstack/run/docker.sock"
    ]
  },
  "commands": {
    "deny": [
      "docker",
      "docker-compose",
      "podman",
      "kubectl"
    ]
  }
}
