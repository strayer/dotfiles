-- settings.lua - Global configuration and font settings

local M = {}

-- Font configuration
M.font = {
  text = "PragmataPro", -- Main font family
  numbers = "PragmataPro", -- Numbers font family
  app_icons = "sketchybar-app-font", -- App icons font family
}

-- Padding and spacing
M.paddings = {
  item_left = 5,
  item_right = 5,
  icon_left = 7,
  icon_right = 4,
  label_left = 4,
  label_right = 7,
}

-- Bar and pill layout (two-pill design)
-- These values are interdependent:
-- - bar_height + bar_y_offset = 38 (aligns with notch bottom)
-- - pill_height = bar_height - 4 (2px spacing top/bottom)
M.layout = {
  bar_height = 35,         -- Total bar height
  bar_y_offset = 3,        -- Top offset (compensates for implicit OS margin)
  bar_padding_h = 5,       -- Horizontal padding (left/right spacing from screen edges)
  pill_height = 31,        -- Pill bracket height
  pill_inner_padding = 4,  -- Padding inside pills (spacing from pill edge to first item)
}

-- Update frequencies (in seconds)
M.update_freq = {
  clock = 10,
  battery = 120,
  network = 30,
  mealplan = 120,
  package_updates = 3600, -- Hourly updates
}

-- System detection
M.hostname = "{{ .chezmoi.hostname }}"
M.is_work_machine = M.hostname == "CO-MBP-KC9KQV64V3"

return M

